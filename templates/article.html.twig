{% extends 'base.html.twig' %}

{% block title %}
    {{ article.title }}
{% endblock %}

{% block main %}
    <article>
        <h2>{{ article.title }}</h2>
        <p>{{ article.content }}</p>
        <img src="{{ article.image }}" alt="{{ article.title }}">
    </article>
{% endblock %}

